export interface StarCreatorCode {
    id: number;
    code: string;
    groupId: number;
    isEnabled: boolean;
}

export interface StarCreatorWelcomeMessage {
    groupId: number;
    welcomeMessage: string;
}
